const num = prompt('Enter a number to check if it is a negative, positive or zero :');

//if (!Math.sign) Math.sign = function(x) { return ((x > 0) - (x < 0)) || +x; }; Old  way
alert(Math.sign(num)) //Pakistani way is smart